package com.example.quickdemozxing;

import android.util.Log;

import androidx.appcompat.app.AlertDialog;

import com.journeyapps.barcodescanner.CaptureActivity;

public class CapturedActivityInformation extends CaptureActivity {
    // This does exactly what the CaptureActivity does,
    // the only difference is the orientation settings. (Manifest file)
}
